programa
{
	
	funcao inicio()
	{
		inteiro base, expoente, resultado
		leia(base)
		leia(expoente)
		resultado = 1
		se(expoente != 1)
		{
			para(inteiro i = 0; i < expoente; i++)
		{
			resultado = resultado * base
		}
		escreva(resultado)
		}
		
	}
}
